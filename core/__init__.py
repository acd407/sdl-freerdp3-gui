"""sdl-freerdp3-gui 的核心层。

* :mod:`core.rdpfile`  — .rdp 文件读写（零依赖，可独立复用）
* :mod:`core.keymap`   — 由 FreeRDP 实测生成的键 → settings 映射
* :mod:`core.schema`   — UI 字段表（分组、标签、控件、默认值）
* :mod:`core.profiles` — 配置的存放与增删查改
* :mod:`core.launch`   — 启动 sdl-freerdp3
"""

__version__ = "0.1.0"
